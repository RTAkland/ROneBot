package {{APP_PACKAGE}};

import cn.rtast.rob.OneBotFactory;
import cn.rtast.rob.event.raw.message.GroupMessage;
import cn.rtast.rob.onebot.OneBotListener;
import org.jetbrains.annotations.NotNull;

public class Main {

    public static class Test implements QQBotListener {
        @Override
        public void onGroupMessageJvm(@NotNull GroupAtMessageCreateEvent message) {
            System.out.println(message);
        }
    }

    public static void main(String[] args) {
        var appId = "111"
        var clientSecret = "222"
        var bot = QBotFactory.createServerJvmBlocking(8080, appId, clientSecret, new Test())
    }
}